#include <stdio.h>
#include <vector>
using namespace std;

class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        for(vector<int>::iterator it = nums.begin(); it != nums.end(); it ++){

        }

        
    }
};

void main(){


}